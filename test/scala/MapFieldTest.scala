import Map.MapField
import org.scalatest.matchers.should
import org.scalatest._
import flatspec._
import matchers._

//class MapFieldTest(n:Int, m:Int, map:Array[Array[Char]], fileName:String)
//  extends MapField(n, m, map, fileName)  with should.Matchers{
//  "A Stack" should "pop values in last-in-first-out order" in {
//   assert(5 === 5)
//  }

//}
